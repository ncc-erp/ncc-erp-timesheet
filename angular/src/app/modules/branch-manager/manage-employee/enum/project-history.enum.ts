export enum ViewDirection {
    Pre = 'pre',
    Next = 'next'
}

export enum DateRangeType {
    Month = 'Month',
    Quarter = 'Quarter',
    Year = 'Year',
    AllTime = 'All Time',
    CustomTime = 'Custom Time'
}

export enum ProjectUserType {
    Member = 0,
    PM = 1,
    Shadow = 2,
    DeActive = 3
}