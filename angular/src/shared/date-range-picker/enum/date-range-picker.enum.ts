export enum DateRangePreset {
  Last7Days = 'last7',
  Last30Days = 'last30',
  ThisWeek = 'thisWeek',
  LastWeek = 'lastWeek',
  ThisMonth = 'thisMonth',
  ThisQuarter = 'thisQuarter',
  ThisYear = 'thisYear'
}

export enum PickerSide {
  Left = 'left',
  Right = 'right',
  Both = 'both'
}

export enum PickerView {
  Days = 'days',
  Months = 'months'
}

export enum NavDirection {
  Prev = 'prev',
  Next = 'next'
}
